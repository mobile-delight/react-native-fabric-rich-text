/**
 * Allowed HTML tags and attributes for sanitization.
 *
 * Re-exports from the single source of truth: constants.ts
 * Native platform constants are generated via: yarn codegen:constants
 */

export { ALLOWED_TAGS, ALLOWED_ATTRIBUTES as ALLOWED_ATTR } from './constants';
